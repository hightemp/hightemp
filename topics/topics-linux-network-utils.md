**1. Диагностика сети:**

*   **Проверка доступности и соединения:**
    *   `ping` - Отправляет ICMP Echo Request пакеты на указанный хост для проверки доступности и измерения времени отклика.
    *   `traceroute` / `tracepath` - Отображает маршрут следования пакетов до указанного хоста, показывая промежуточные узлы и время отклика на каждом.
    *   `mtr` - Комбинирует функциональность `ping` и `traceroute`, отображая статистику потерь и задержек на каждом узле маршрута в реальном времени.
    *   `arping` - Отправляет ARP-запросы для определения доступности хоста в локальной сети по MAC-адресу.

*   **Работа с DNS:**
    *   `nslookup` - Интерактивный инструмент для запросов к DNS-серверам. Позволяет получать различные типы записей (A, MX, CNAME и т.д.).
    *   `dig` (Domain Information Groper) - Более продвинутый инструмент для запросов к DNS-серверам, предоставляющий подробную информацию о DNS-ответах.
    *   `host` - Простая утилита для преобразования доменных имен в IP-адреса и наоборот.

*   **Анализ сетевого трафика и соединений:**
    *   `tcpdump` - Мощный инструмент для перехвата и анализа сетевого трафика. Позволяет фильтровать трафик по различным критериям (протокол, порт, IP-адрес и т.д.) и сохранять его в файл для последующего анализа.
    *   `wireshark` - Графический анализатор сетевого трафика, предоставляющий удобный интерфейс для просмотра и анализа перехваченных пакетов.
    *   `tshark` - Консольная версия Wireshark.
    *   `netstat` / `ss` - Отображают информацию о сетевых соединениях, таблицах маршрутизации, статистике интерфейсов и открытых портах. `ss` является более современной и рекомендуемой альтернативой `netstat`.
    *   `lsof` - Показывает список открытых файлов и связанных с ними процессов, включая сетевые соединения.

*   **Сканирование портов и служб:**
    *   `nmap` (Network Mapper) - Мощный инструмент для сканирования портов, обнаружения сетевых служб, определения операционных систем и многого другого.
    *   `nc` / `netcat` - "Швейцарский нож" для работы с сетевыми соединениями. Позволяет устанавливать соединения, передавать данные, прослушивать порты и многое другое.

*   **Измерение производительности сети:**
    *   `iperf` / `iperf3` - Инструмент для измерения пропускной способности сети между двумя узлами.
    *   `speedtest-cli` - Консольный клиент для популярного сервиса Speedtest.net, позволяющий измерить скорость интернет-соединения.

**2. Настройка сети:**

*   **Управление сетевыми интерфейсами:**
    *   `ifconfig` (устаревает) - Позволяет просматривать и настраивать параметры сетевых интерфейсов (IP-адрес, маска сети, шлюз и т.д.).
    *   `ip` - Современная утилита для управления сетевыми интерфейсами, маршрутизацией и другими сетевыми настройками. Заменяет `ifconfig`, `route` и другие устаревшие утилиты.
        *   `ip addr` - Управление адресами интерфейсов.
        *   `ip link` - Управление состоянием интерфейсов.
        *   `ip route` - Управление таблицей маршрутизации.
    *   `ifup` / `ifdown` - Скрипты для активации и деактивации сетевых интерфейсов, обычно используемые системой инициализации.

*   **Беспроводные сети:**
    *   `iwconfig` (устаревает) - Утилита для настройки беспроводных сетевых интерфейсов (SSID, режим работы, ключ шифрования и т.д.).
    *   `iw` - Современная утилита для управления беспроводными интерфейсами, заменяющая `iwconfig`.
    *   `wpa_supplicant` - Утилита для подключения к беспроводным сетям, защищенным с помощью WPA/WPA2/WPA3.

*   **DHCP:**
    *   `dhclient` - Клиент DHCP, используемый для автоматического получения IP-адреса и других сетевых настроек от DHCP-сервера.

*   **Межсетевые экраны:**
    *   `iptables` - Утилита для управления межсетевым экраном Netfilter, встроенным в ядро Linux. Позволяет создавать правила фильтрации сетевого трафика.
    *   `nftables` - Современная система фильтрации пакетов, призванная заменить `iptables`. Предоставляет более гибкий и мощный набор функций.
    *   `firewalld` - Динамический демон управления межсетевым экраном, использующий зоны и сервисы для упрощения настройки.

*   **Туннелирование и VPN:**
    *   `OpenVPN` - Популярное решение для создания защищенных VPN-соединений.
    *   `WireGuard` - Современный, высокопроизводительный и безопасный VPN-протокол.
    *   `ssh` - Может использоваться для создания зашифрованных туннелей (SSH-туннелирование).

**3. Удаленный доступ:**

*   `ssh` (Secure Shell) - Протокол и набор утилит для безопасного удаленного доступа к системам. Обеспечивает шифрование трафика и аутентификацию пользователей.
    *   `ssh` - клиент для подключения к удаленным серверам.
    *   `sshd` - демон, реализующий SSH-сервер.
    *   `ssh-keygen` - утилита для генерации ключей SSH.
    *   `ssh-agent` - агент для хранения ключей SSH в памяти.
*   `scp` (Secure Copy) - Утилита для безопасного копирования файлов между хостами по протоколу SSH.
*   `sftp` (Secure File Transfer Protocol) - Протокол и набор утилит для безопасной передачи файлов, аналогичный FTP, но работающий поверх SSH.
*   `rsync` - Утилита для синхронизации файлов и каталогов между локальными и удаленными системами. Может использовать SSH для безопасной передачи данных.
*   `telnet` (устаревает) - Протокол и утилита для удаленного доступа к системам. Не обеспечивает шифрование трафика, поэтому использовать его не рекомендуется.

**4. Сетевые службы (Серверы):**

*   **Веб-серверы:**
    *   `Apache` - Один из самых популярных веб-серверов в мире.
    *   `Nginx` - Высокопроизводительный веб-сервер и обратный прокси-сервер.

*   **Почтовые серверы:**
    *   `Postfix` - Популярный почтовый сервер (MTA).
    *   `Sendmail` - Один из старейших почтовых серверов.
    *   `Dovecot` - IMAP и POP3 сервер.

*   **DNS-серверы:**
    *   `BIND` (Berkeley Internet Name Domain) - Наиболее распространенный DNS-сервер.

*   **FTP-серверы:**
    *   `vsftpd` (Very Secure FTP Daemon) - Безопасный и быстрый FTP-сервер.
    *   `ProFTPD` - Модульный и настраиваемый FTP-сервер.

*   **Файловые серверы:**
    *   `Samba` - Позволяет предоставлять доступ к файлам и принтерам для клиентов Windows по протоколу SMB/CIFS.
    *   `NFS` (Network File System) - Протокол и набор утилит для предоставления доступа к файлам и каталогам по сети.

**5. Прочие утилиты:**

*   **Загрузка файлов:**
    *   `wget` - Консольная утилита для загрузки файлов из сети по протоколам HTTP, HTTPS и FTP.
    *   `curl` - Мощная утилита для передачи данных по различным протоколам (HTTP, HTTPS, FTP, SFTP, SCP и т.д.). Часто используется для тестирования API и взаимодействия с веб-сервисами.

*   **Информация о доменах и IP-адресах:**
    *   `whois` - Утилита для получения информации о регистрации доменных имен и IP-адресов.

*   **Сетевые файловые системы:**
    *   `sshfs` - Позволяет монтировать удаленные каталоги через SSH.

*   **Мониторинг:**
    *   `iftop` - Отображает использование полосы пропускания сетевыми интерфейсами в реальном времени.
    *   `nethogs` - Показывает, какие процессы используют сетевое соединение и сколько трафика они потребляют.

---

**1. Диагностика и мониторинг сети:**

*   **ping:** Проверка доступности узла и измерение времени ответа.
*   **traceroute/tracepath:** Отслеживание маршрута пакетов.
*   **mtr (My Traceroute):** Комбинация `ping` и `traceroute`.
*   **netstat/ss:** Информация о сетевых соединениях, таблицах маршрутизации.
*   **tcpdump:** Захват и анализ сетевого трафика.
*   **nmap (Network Mapper):** Сканер портов и сетевой разведчик.
*   **iftop:** Мониторинг использования полосы пропускания в реальном времени.
*   **iptraf-ng:** Интерактивный монитор сетевого трафика.
*   **bmon:** Мониторинг пропускной способности с графиками.
*   **slurm:** Монитор сетевого трафика (альтернатива `bmon`).
*   **speedometer:** Отображение скорости передачи данных.
*   **vnstat:** Сбор статистики использования трафика.
*   **tcptrack:** Отслеживание состояния TCP-соединений.
*   **iperf/iperf3:** Измерение пропускной способности сети.
*   **ethtool:** Отображение и изменение параметров сетевых интерфейсов.
*   **whois:** Получение информации о доменном имени или IP-адресе.
*   **dig (Domain Information Groper):** Запросы к DNS-серверам.
*   **nslookup:** Запросы к DNS-серверам (альтернатива `dig`).
*   **host:** Преобразование доменного имени в IP-адрес и наоборот.
*   **arp:** Просмотр и изменение ARP-таблицы.
*   **fping:** Пингование нескольких хостов одновременно.
*   **hping3:** Создание и отправка произвольных сетевых пакетов.
*   **pathping:** Комбинация `ping` и `traceroute` со статистикой потерь.
*   **nping:** Генератор пакетов, входит в состав Nmap.
*   **arping:** Отправка ARP-запросов.
*   **nethogs:** Определение процессов, использующих сетевое соединение.
*   **gnutls-cli:** Тестирование соединений на основе протокола TLS
*   **swaks:** Тестирование SMTP протокола
*   **dnsdiag:** Набор инструментов для диагностики DNS
*   **fierce:** Инструмент для поиска поддоменов
*   **massdns:** Быстрый резолвер DNS
*   **netdiag:** Скрипт на языке Python для диагностики сети
*   **p0f:** Инструмент для пассивной идентификации ОС, фаервола и т.д.
*   **tcptraceroute:**  traceroute, использующая TCP пакеты
*   **lsof:** Позволяет узнать какие открыты порты, какими программами.
*   **tshark:** Это консольная версия Wireshark
*   **Termshark:** tui интерфейс для tshark
*   **dsniff:** Набор утилит для перехвата различной информации в сети.

**2. Настройка и управление сетью:**

*   **ip:** Настройка сетевых интерфейсов, маршрутизации, туннелей.
*   **ifconfig (устаревает):** Настройка сетевых интерфейсов.
*   **route (устаревает):** Настройка таблицы маршрутизации.
*   **iwconfig:** Настройка беспроводных сетевых интерфейсов.
*   **iwlist:** Информация о беспроводных сетях.
*   **iw:** Современная утилита для управления беспроводными интерфейсами.
*   **nmcli (Network Manager CLI):** Управление Network Manager.
*   **nmtui (Network Manager TUI):** Текстовый интерфейс для Network Manager.
*   **hostnamectl:** Просмотр и изменение имени хоста.
*   **systemd-networkd:** Демон для управления сетевыми интерфейсами.
*   **ifupdown:** Система управления сетевыми интерфейсами.
*   **iptables/nftables:** Настройка брандмауэра.
*   **firewall-cmd:** Управление firewalld.
*   **ufw (Uncomplicated Firewall):** Простой интерфейс для `iptables`.
*   **bridge-utils:** Создание и управление мостами (bridge).
*   **ip link:** Управление сетевыми интерфейсами, создание виртуальных интерфейсов.
*   **tc (Traffic Control):** Управление трафиком, QoS, шейпинг.
*   **vconfig:** Позволяет настраивать VLAN теги на сетевом интерфейсе.
*   **mii-tool:** Позволяет управлять MII (Media Independent Interface)
*   **wavemon:** Консольная утилита для мониторинга беспроводных устройств
*   **NetworkManager:** Демон для управления сетевыми подключениями.
*   **connman:** Еще один демон для управления сетевыми подключениями.

**3. Передача файлов и удаленный доступ:**

*   **scp (Secure Copy):** Копирование файлов по SSH.
*   **sftp (Secure FTP):** Интерактивный FTP-клиент поверх SSH.
*   **rsync:** Синхронизация файлов и каталогов.
*   **ssh (Secure Shell):** Зашифрованный удаленный доступ к командной строке.
*   **telnet (устаревает):** Удаленный доступ к командной строке (незашифрованный).
*   **ftp (File Transfer Protocol) (устаревает):** Передача файлов (незашифрованная).
*   **nc (Netcat):** Работа с TCP и UDP соединениями.
*   **wget:** Скачивание файлов из Интернета.
*   **curl:** Передача данных по различным протоколам.
*   **aria2:** Консольный менеджер загрузок.
*   **axel:**  Еще один консольный ускоритель загрузок.
*   **mosh:**  (Mobile Shell) альтернатива SSH, лучше работает в условиях плохого соединения
*   **tmate:** Позволяет расшарить консоль через интернет
*   **ngrok:** Инструмент для создания туннелей из интернета в локальную сеть

**4. Сетевые службы и серверы:**

*   **OpenSSH (sshd):** Сервер SSH.
*   **vsftpd, ProFTPD, Pure-FTPd:** Серверы FTP.
*   **Apache, Nginx:** Веб-серверы.
*   **Samba:** Реализация протокола SMB/CIFS.
*   **NFS (Network File System):** Сетевая файловая система.
*   **BIND, dnsmasq, Unbound:** DNS-серверы.
*   **dhcpd, dnsmasq:** DHCP-серверы.
*   **ISC DHCP server:** Один из самых распостраненных DHCP серверов.
*   **Postfix, Exim, Sendmail:** Почтовые серверы (MTA).
*   **Dovecot, Courier:** Серверы IMAP и POP3 (MDA).
*   **OpenVPN:**  Реализация VPN.
*   **WireGuard:** Современный и быстрый VPN протокол.
*   **strongSwan:**  Еще одна реализация IPsec
*   **freeradius:** Сервер аутентификации RADIUS
*   **squid:** Прокси сервер
*   **haproxy:** Балансировщик нагрузки
*   **keepalived:** Обеспечивает отказоустойчивость за счет VRRP протокола
*   **ntp, openntpd, chrony:** Серверы и клиенты для синхронизации времени

**5. Мониторинг и анализ трафика (более детально):**

*   **Wireshark:**  Графический анализатор трафика (есть консольный аналог - `tshark`).
*   **tcpflow:** Реконструкция TCP-потоков.
*   **ngrep:** Поиск по сетевому трафику, аналог `grep`.
*   **darkstat:** Собирает различную статистику по трафику.
*   **pktstat:** Отображает статистику по сетевым соединениям в реальном времени.
*   **jnettop:** Еще один инструмент для мониторинга трафика.
*   **trafshow:** Показывает текущие соединения и их трафик
*   **NetHogs:**  Показывает трафик по процессам
*   **cbm:** (Color Bandwidth Meter) простенький показометр трафика по интерфейсам
*   **traceroute-nanog:** Более продвинутый вариант traceroute
*   **xprobe2:** Инструмент для удаленного определения ОС по отпечаткам
*   **snoop:** Устаревший, но все еще встречающийся сниффер

**6. Безопасность и аудит:**

*   **aircrack-ng:** Аудит безопасности беспроводных сетей.
*   **kismet:** Обнаружение беспроводных сетей, сниффер, IDS.
*   **snort:** Система обнаружения и предотвращения вторжений (IDS/IPS).
*   **suricata:** Еще одна мощная IDS/IPS.
*   **nmap:** (уже упоминался) активно используется для аудита безопасности.
*   **nikto:** Сканер уязвимостей веб-серверов.
*   **hydra:** Инструмент для брутфорса паролей к различным сервисам.
*   **john:** (John the Ripper) инструмент для взлома паролей.
*   **lynis:** Инструмент для аудита безопасности системы
*   **chkrootkit:** Инструмент для поиска руткитов
*   **rkhunter:** Еще один инструмент для поиска руткитов

**7. Туннелирование и прокси:**

*   **socat:** Многофункциональный инструмент для работы с сокетами, может использоваться для туннелирования.
*   **stunnel:** Создание SSL-туннелей.
*   **sshuttle:** VPN на основе SSH.
*   **openconnect:** Клиент для подключения к VPN-серверам Cisco AnyConnect.
*   **proxychains:** Позволяет запускать программы через прокси-сервер.
*   **tor:** Клиент и сервер анонимной сети Tor.
*   **iodine:** Инструмент для туннелирования трафика через DNS.

**8. Инструменты для работы с IPv6:**

*   Большинство современных утилит поддерживают IPv6, но есть и специфичные:
*   **ndisc6:** Набор утилит для работы с IPv6, включает `ndisc6`, `rdisc6`, `tcpspray6`.
*   **ipv6calc:** Инструмент для работы с IPv6 адресами
*   **6tunnel:** Позволяет туннелировать IPv6 трафик через IPv4
*   **thc-ipv6:** Набор инструментов для тестирования и атак на IPv6 сети

**9. Специализированные и прочие утилиты:**

*   **arpwatch:** Мониторинг ARP-трафика и обнаружение изменений.
*   **rwho:** Показывает, кто залогинен на удаленных машинах (устаревает).
*   **rsh:** Удаленное выполнение команд (устаревает, используйте SSH).
*   **rexec:** Еще одна устаревшая утилита для удаленного выполнения команд.
*   **bwm-ng:** (Bandwidth Monitor NG) простой монитор пропускной способности.
*   **tcptraceroute:** `traceroute` с использованием TCP-пакетов.
*   **dnschef:** Инструмент для тестирования DNS.
*   **nbtscan:** Сканер NetBIOS имен.
*   **smbclient:** Клиент для доступа к ресурсам Windows (SMB/CIFS).
*   **nfsstat:** Статистика по NFS.
*   **rpcinfo:** Информация о службах RPC.
*   **knockd:** Демон, реализующий "port knocking".
*   **conntrack:** Утилита для просмотра и управления таблицей conntrack
*   **ssldump:** Инструмент для перехвата SSL/TLS трафика
*   **tcpreplay:** Инструмент для воспроизведения записанного трафика
*   **vconfig:** Позволяет настраивать VLAN теги на сетевом интерфейсе
*   **Mii-tool:** Позволяет управлять MII (Media Independent Interface)
*   **Wavemon:** Консольная утилита для мониторинга беспроводных устройств
*   **Netdiscover:** Инструмент для обнаружения хостов в локальной сети на основе ARP-запросов
*   **Xprobe2:** Инструмент для удаленного определения ОС по отпечаткам
*   **Snoop:** Устаревший, но все еще встречающийся сниффер




